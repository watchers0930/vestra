import ExpertClient from "./ExpertClient";

export const metadata = {
  title: "전문가 연결 - VESTRA",
  description: "부동산 전문가와 1:1 상담을 연결해 드립니다",
};

export default function ExpertRenewalPage() {
  return <ExpertClient />;
}
