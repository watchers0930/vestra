import ContractRenewalClient from "./ContractRenewalClient";

export const metadata = {
  title: "계약검토 - VESTRA",
  description: "부동산 계약서 AI 검토 서비스",
};

export default function ContractRenewalPage() {
  return <ContractRenewalClient />;
}
